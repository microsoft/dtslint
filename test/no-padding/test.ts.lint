function f() {
             ~ [0 % ("after '{'")]

    return [
           ~ [0 % ("after '['")]

        f(
         ~ [0 % ("after '('")]

            0

        )
        ~ [0 % ("before ')'")]

    ];
    ~ [0 % ("before ']'")]

}
~ [0 % ("before '}'")]

function f() {
    return [
        f(
            0
        )
    ];
}

[0]: Don't leave a blank line %s. See: https://github.com/Microsoft/dtslint/blob/master/docs/no-padding.md